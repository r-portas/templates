{
  "name": "templates",
  "private": true,
  "workspaces": [
    "templates/*",
    "docs"
  ],
  "scripts": {
    "update": "bun update -r -i --latest",
    "build": "bun run --workspaces build",
    "lint": "bun run --workspaces lint"
  }
}
