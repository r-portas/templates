{
  "name": "templates",
  "private": true,
  "workspaces": [
    "templates/*",
    "docs"
  ],
  "scripts": {
    "update": "bun run --workspaces update",
    "build": "bun run --workspaces build",
    "lint": "bun run --workspaces lint"
  }
}
